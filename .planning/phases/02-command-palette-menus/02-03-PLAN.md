---
phase: 02-command-palette-menus
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent86/tui/screens/model_picker.py
  - src/agent86/tui/screens/mode_picker.py
  - tests/tui/test_pickers.py
autonomous: true
requirements: [TUI-04]
user_setup: []

must_haves:
  truths:
    - "Selecting a mode in an arrow-key RadioSet picker resolves to one of the exact strings 'ask'/'auto'/'deny'"
    - "Selecting a model in an arrow-key OptionList picker resolves to a 'provider:model' ref string"
    - "Escape (or explicit cancel) dismisses each picker with None and never leaves the worker/UI hanging"
    - "The /model picker list is built from the three config role slots (default, route.cheap, route.frontier), deduped by ref, with a defensive empty-list fallback"
  artifacts:
    - path: "src/agent86/tui/screens/mode_picker.py"
      provides: "ModePickerModal(ModalScreen[str | None]) — RadioSet ask/auto/deny"
      contains: "class ModePickerModal"
    - path: "src/agent86/tui/screens/model_picker.py"
      provides: "ModelPickerModal(ModalScreen[str | None]) + model_choices(cfg) helper (D-12)"
      contains: "def model_choices"
    - path: "tests/tui/test_pickers.py"
      provides: "Pilot tests: each picker dismisses with the selected value and with None on cancel"
      contains: "test_mode_picker"
  key_links:
    - from: "ModePickerModal.on_radio_set_changed"
      to: "self.dismiss(<value str>)"
      via: "str(event.pressed.label)"
      pattern: "dismiss\\("
    - from: "ModelPickerModal.on_option_list_option_selected"
      to: "self.dismiss(event.option_id)"
      via: "OptionList.OptionSelected"
      pattern: "on_option_list_option_selected"
---

<objective>
Build the two arrow-key choice pickers (D-07/D-08) as `ModalScreen[str | None]` subclasses that
mirror the existing `ApprovalModal` pattern exactly, plus the `model_choices(cfg)` helper that
sources the `/model` list from today's config (D-12). These are standalone new files under
`tui/screens/` with no dependency on commands.py or app.py — they run parallel with Plans 01/02.
Plan 04 imports and chains into them.

Purpose: Deliver the TUI-04 arrow-key selection surfaces (mode RadioSet, model OptionList) as
self-contained, individually testable modals.
Output: `screens/mode_picker.py`, `screens/model_picker.py`, `tests/tui/test_pickers.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-command-palette-menus/02-CONTEXT.md
@.planning/phases/02-command-palette-menus/02-RESEARCH.md

<interfaces>
Mirror this proven repo pattern (src/agent86/tui/screens/approval.py):
```python
class ApprovalModal(ModalScreen[bool]):
    BINDINGS = [("escape", "deny", "Deny")]
    def compose(self) -> ComposeResult: ...   # Container(id="approval-dialog") + widgets
    def on_button_pressed(self, event): self.dismiss(...)
    def action_deny(self): self.dismiss(False)
```
Config shape the model picker reads (src/agent86/config.py):
```python
class ModelRoute(BaseModel):
    cheap: str = "ollama:llama3.1"
    frontier: str = "anthropic:claude-opus-4-8"
class ModelConfig(BaseModel):
    default: str = "anthropic:claude-opus-4-8"
    route: ModelRoute = ...
class Config(BaseModel):
    model: ModelConfig
    providers: dict[str, ProviderConfig]
```
Mode values accepted by policy.parse_mode: the lowercase strings "ask", "auto", "deny".
Textual imports: `from textual.screen import ModalScreen`, `from textual.widgets import Label,
RadioButton, RadioSet, OptionList`, `from textual.widgets.option_list import Option`,
`from textual.containers import Container`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ModePickerModal — RadioSet of ask/auto/deny</name>
  <files>src/agent86/tui/screens/mode_picker.py</files>
  <read_first>
    - src/agent86/tui/screens/approval.py (ModalScreen[bool] pattern to mirror exactly)
    - src/agent86/guardrails/policy.py (parse_mode — the exact strings the picker must emit)
    - .planning/phases/02-command-palette-menus/02-RESEARCH.md (Pattern 4, Pitfall 4)
  </read_first>
  <behavior>
    - ModePickerModal("ask") composes a RadioSet with buttons "ask"/"auto"/"deny", "ask" pre-pressed
    - Selecting a radio dismisses with the exact lowercase string (e.g. "auto"), never an ApprovalMode enum
    - Escape dismisses with None
  </behavior>
  <action>
    Create `ModePickerModal(ModalScreen[str | None])`:
    - `BINDINGS = [("escape", "cancel", "Cancel")]`
    - `__init__(self, current: str)` stores `self._current = current`.
    - `compose`: `with Container(id="mode-picker-dialog"):` yield `Label("Approval mode")`, then
      `with RadioSet(id="mode-options"):` yield `RadioButton(v, value=(v == self._current))` for v in
      `("ask", "auto", "deny")`.
    - `on_radio_set_changed(self, event: RadioSet.Changed)`: `self.dismiss(str(event.pressed.label))`.
      (Per Pitfall 4: emit the label string directly — do NOT compare against ApprovalMode members.)
    - `action_cancel(self)`: `self.dismiss(None)`.
    - `__all__ = ["ModePickerModal"]`.
    All Textual imports are module-local to this file (stays within tui/, preserving the lazy-import
    contract — nothing here is imported by cli.py at module load).
  </action>
  <verify>
    <automated>pytest tests/tui/test_pickers.py -x -q -k mode</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class ModePickerModal(ModalScreen\[str | None\])" src/agent86/tui/screens/mode_picker.py` matches
    - `grep -n "RadioSet\|RadioButton" src/agent86/tui/screens/mode_picker.py` matches
    - `grep -n "self.dismiss(str(event.pressed.label))" src/agent86/tui/screens/mode_picker.py` matches
    - `pytest tests/tui/test_pickers.py -x -q -k mode` exits 0
  </acceptance_criteria>
  <done>ModePickerModal renders a RadioSet, emits the exact 'ask'/'auto'/'deny' string on selection, and dismisses None on Escape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ModelPickerModal + model_choices(cfg) source helper (D-12)</name>
  <files>src/agent86/tui/screens/model_picker.py</files>
  <read_first>
    - src/agent86/tui/screens/approval.py (ModalScreen pattern)
    - src/agent86/config.py (lines 38-50: ModelRoute / ModelConfig role slots)
    - .planning/phases/02-command-palette-menus/02-RESEARCH.md (Pattern 4, Open Question 3, D-12)
  </read_first>
  <behavior>
    - model_choices(cfg) returns list[tuple[label, value]] from the three role slots
      (cfg.model.default, cfg.model.route.cheap, cfg.model.route.frontier), deduped by value(ref)
    - When two roles share a ref, the label aggregates the role names (e.g. "default, frontier — anthropic:claude-opus-4-8")
    - With default config (all three slots non-empty) the list has >= 1 entry and no duplicate values
    - ModelPickerModal(choices) OptionList; selecting an option dismisses with that option's value (the ref)
    - Escape dismisses with None
  </behavior>
  <action>
    Create module-level `def model_choices(cfg) -> list[tuple[str, str]]`:
    - Iterate role slots in order: `("default", cfg.model.default)`, `("route.cheap", cfg.model.route.cheap)`,
      `("route.frontier", cfg.model.route.frontier)`.
    - Dedup by the ref string, preserving first-seen order; accumulate the role names sharing a ref.
    - Build label as `f"{', '.join(roles)} — {ref}"`, value is the ref string.
    - Return `[]` if no non-empty refs (defensive D-12 fallback — Plan 04 uses [] to prefill "/model ").

    Create `ModelPickerModal(ModalScreen[str | None])`:
    - `BINDINGS = [("escape", "cancel", "Cancel")]`
    - `__init__(self, choices: list[tuple[str, str]])` stores `self._choices`.
    - `compose`: `with Container(id="model-picker-dialog"):` yield `Label("Switch model")`, then
      `yield OptionList(*[Option(label, id=value) for label, value in self._choices])`.
    - `on_option_list_option_selected(self, event: OptionList.OptionSelected)`: `self.dismiss(event.option_id)`.
    - `action_cancel(self)`: `self.dismiss(None)`.
    - `__all__ = ["ModelPickerModal", "model_choices"]`.
  </action>
  <verify>
    <automated>pytest tests/tui/test_pickers.py -x -q -k model</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def model_choices" src/agent86/tui/screens/model_picker.py` matches
    - `grep -n "class ModelPickerModal(ModalScreen\[str | None\])" src/agent86/tui/screens/model_picker.py` matches
    - `grep -n "on_option_list_option_selected" src/agent86/tui/screens/model_picker.py` matches
    - `grep -n "self.dismiss(event.option_id)" src/agent86/tui/screens/model_picker.py` matches
    - `pytest tests/tui/test_pickers.py -x -q -k model` exits 0
  </acceptance_criteria>
  <done>model_choices dedupes the three role refs into (label, value) pairs with a []-fallback; ModelPickerModal dismisses with the selected ref or None.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pilot tests for both pickers</name>
  <files>tests/tui/test_pickers.py</files>
  <read_first>
    - tests/tui/test_app.py (Pilot harness: App.run_test(), pilot.press/click, _wait_until)
    - src/agent86/tui/screens/model_picker.py, src/agent86/tui/screens/mode_picker.py (post Tasks 1-2)
    - src/agent86/config.py (load_config for building a real cfg)
  </read_first>
  <behavior>
    - test_mode_picker_dismisses_with_selected_value: pushing ModePickerModal and selecting "auto" yields "auto"
    - test_mode_picker_cancel_returns_none: Escape yields None
    - test_model_picker_dismisses_with_ref: selecting an option yields the ref string
    - test_model_choices_dedups_roles: model_choices(load_config()) has unique values and >=1 entry
  </behavior>
  <action>
    Create `tests/tui/test_pickers.py`. Because ModalScreen needs a host app, build a tiny host
    `App` in-test whose `on_mount` does `self.push_screen(picker, self._store)` where `_store`
    saves the dismissed value to `self.result`; or drive via `app.push_screen_wait` inside a worker
    — use whichever matches the repo's existing modal test style in test_app.py (`push_screen` +
    callback, then `pilot.press`/`pilot.click`, then assert on the stored result).
    - test_mode_picker_dismisses_with_selected_value: focus the RadioSet, press "down" to move to
      "auto", press "enter" (or click the radio) and assert stored result == "auto".
    - test_mode_picker_cancel_returns_none: press "escape", assert stored result is None.
    - test_model_picker_dismisses_with_ref: pass `model_choices(load_config())`; select the first
      option; assert the stored result equals that option's ref value.
    - test_model_choices_dedups_roles: `vals = [v for _, v in model_choices(load_config())]`; assert
      `len(vals) == len(set(vals))` and `len(vals) >= 1`.
  </action>
  <verify>
    <automated>pytest tests/tui/test_pickers.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_mode_picker_dismisses_with_selected_value" tests/tui/test_pickers.py` matches
    - `grep -n "def test_model_choices_dedups_roles" tests/tui/test_pickers.py` matches
    - `pytest tests/tui/test_pickers.py -x -q` exits 0
  </acceptance_criteria>
  <done>Both pickers are covered by green Pilot tests for the select and cancel paths, and model_choices dedup is pinned.</done>
</task>

</tasks>

<verification>
- `pytest tests/tui/test_pickers.py -x -q` exits 0
- `pytest tests/tui/test_lazy_import.py -x -q` exits 0 (new screens stay inside tui/, no cli.py import)
- `grep -rn "class Mode\|class Model" src/agent86/tui/screens/*.py` shows both picker classes
</verification>

<validation>
Dimension 8 — concrete checks for TUI-04 (arrow-key menus):
- `pytest tests/tui/test_pickers.py::test_mode_picker_dismisses_with_selected_value -x` — mode RadioSet
- `pytest tests/tui/test_pickers.py::test_model_picker_dismisses_with_ref -x` — model OptionList
- `pytest tests/tui/test_pickers.py::test_model_choices_dedups_roles -x` — D-12 source correctness
</validation>

<success_criteria>
Two arrow-key `ModalScreen[str | None]` pickers exist mirroring ApprovalModal; the mode picker emits
'ask'/'auto'/'deny', the model picker emits a config-sourced 'provider:model' ref; both cancel to None;
model_choices dedupes the role slots with a []-fallback. All picker tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-command-palette-menus/02-03-SUMMARY.md` documenting
the picker constructor signatures (`ModePickerModal(current: str)`, `ModelPickerModal(choices)`),
the `model_choices(cfg)` return shape, and the dismiss contracts for Plan 04's chaining callbacks.
</output>
